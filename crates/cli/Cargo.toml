[package]
name = "cryst"
version = "0.8.0"
edition = "2024"
license = "MIT"

[dependencies]
engine = { path = "../engine" }
tui = { path = "../tui" }
crossterm = "0.29"
rand = "0.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_ignored = "0.1"
clap = { version = "4.5", features = ["derive"] }
clap_complete = "4.5"
