[package]
name = "cryst"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
engine = { path = "../engine" }
tui = { path = "../tui" }
crossterm = "0.27"
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_ignored = "0.1"
